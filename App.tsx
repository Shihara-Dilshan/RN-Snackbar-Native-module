import React from 'react';
import {
  SafeAreaView,
  StatusBar,
  StyleSheet,
  Text,
} from 'react-native';

const App = () => {
  return (
    <SafeAreaView>
      <StatusBar  />
        <Text>init</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({

});

export default App;
